<%= form_for(@call_escalation) do |f| %>
  <% if @call_escalation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@call_escalation.errors.count, "error") %> prohibited this call_escalation from being saved:</h2>

      <ul>
      <% @call_escalation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="field">
    <%= f.label :position %><br />
    <%= f.number_field :position %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
