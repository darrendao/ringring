<%= form_for([@call_list, CallEscalation.new], :remote => true) do |f| %>
  User: <%=  f.select 'user_id', oncall_assignment_options %>
  Retry: <%= f.text_field 'retry' %>
  <%= submit_tag "Add" %>
  | <%= link_to_function 'Done', "$('#add_entry').hide(); $('#add_entry_link').show()" %>
<% end %>
